{
  "name": "odoo-sync-worker",
  "version": "1.0.0",
  "description": "GCP worker: sync project.task attachments from source Odoo to target Odoo DBs (Taxes + Onboarding)",
  "main": "src/index.js",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/index.js",
    "sync": "node src/cli-sync.js",
    "debug": "node src/debug-sync.js",
    "test": "node --test src/test/ 2>/dev/null || true"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.0.0",
    "dotenv": "^16.0.0"
  }
}
